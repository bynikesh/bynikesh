import React from "react"
import { Link } from "gatsby"

import Layout from "../components/layout"

const blog = () => (
  <Layout>
    <div
      className="page"
      style={{
        opacity: "0.7",
        marginLeft: "145px",
      }}
      data-simplebar
    >
      <h1>sorry no post at the moment</h1>
    </div>
  </Layout>
)

export default blog
